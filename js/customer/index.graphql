type Customer {
  email: String
  name: String
}

type Query {
  getCustomerById(id:ID!): Customer
    @rest(
      endpoint: "https://friday-8bf41-default-rtdb.firebaseio.com/customers/$id.json"
    )
}